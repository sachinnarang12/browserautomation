version: "3.8"

services:
  portal:
    build: .
    container_name: automateportal
    ports:
      - "5000:5000"
      - "6080:6080"       # noVNC - live browser monitoring
    env_file:
      - .env                          # Create from .env.example
    environment:
      - FLASK_ENV=production
      - AUTOMATEPORTAL_EDITION=${AUTOMATEPORTAL_EDITION:-standard}
    volumes:
      # Persist data across container restarts (secrets, DB, config)
      - portal-data:/app/data
    restart: unless-stopped

volumes:
  portal-data:
